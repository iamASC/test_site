from django import forms
from to_do_list.models import Category
"""
class CategoryForm(forms.Form):
    class Meta:
        model = Category
    cat_name = forms.CharField(max_length=40,label='Название')
"""